// Generated by CoffeeScript 1.3.3
(function() {
  var ServerAction, fs, htmlEntryPoint, http, io, network;

  http = require('http');

  fs = require('fs');

  io = require('socket.io');

  ServerAction = require('./server');

  htmlEntryPoint = http.createServer(function(req, res) {
    return fs.readFile('../test/index.html', 'utf-8', function(error, content) {
      res.writeHead(200, {
        'Content-Type': 'text/html'
      });
      return res.end(content);
    });
  });

  network = io.listen(htmlEntryPoint);

  network.sockets.on('connection', function(socket) {
    return socket.on('initialize', function(data) {
      var action;
      action = new ServerAction(network.sockets);
      try {
        action.joinRoom(socket, data.roomId);
        return network.sockets["in"](data.roomId).emit('room-joined', {});
      } catch (error) {
        socket.emit('room-deny', error.toObject());
        return console.log(error);
      }
    });
  });

  htmlEntryPoint.listen(80);

}).call(this);
